const fs = require('fs');
const path = require('path');

// A very simple way to "peek" at a PNG file for colors isn't easy without libraries.
// I'll try to check if there's any other way or just use the browser tool again with a screenshot.
// Since the browser tool had environment issues, I'll try to use powershell to get some info.
